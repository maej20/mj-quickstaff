Config = {
  ToggleCommand = 'staff', -- Sets the command used to toggle staff mode on/off. (default = "staff")
  AutoGodMode = true, -- Turns on godmode while in staff mode. (default = true)
  AutoPlayerIds = true, -- Automatically show Player ID's over player's head while in staff mode. (default = true)
  ReturnToLastLocation = true -- Returns staff players to their "pre-staff mode" location upon exiting staff mode. (default = true)
}